{% extends 'base.html.twig' %}

{% block title %}Registro{% endblock %}

{% block body %}
    <h1>Registro</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ registro.id }}</td>
            </tr>
            <tr>
                <th>Input</th>
                <td>{{ registro.input ? registro.input|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Output</th>
                <td>{{ registro.output ? registro.output|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('registro_index') }}">back to list</a>

    <a href="{{ path('registro_edit', {'id': registro.id}) }}">edit</a>

    {{ include('registro/_delete_form.html.twig') }}
{% endblock %}
