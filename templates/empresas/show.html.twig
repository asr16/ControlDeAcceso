{% extends 'base.html.twig' %}

{% block title %}Empresas{% endblock %}

{% block body %}
    <h1>Empresas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ empresa.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ empresa.name }}</td>
            </tr>
            <tr>
                <th>Cif</th>
                <td>{{ empresa.cif }}</td>
            </tr>
            <tr>
                <th>Direction</th>
                <td>{{ empresa.direction }}</td>
            </tr>
            <tr>
                <th>Cp</th>
                <td>{{ empresa.cp }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ empresa.city }}</td>
            </tr>
            <tr>
                <th>Province</th>
                <td>{{ empresa.province }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ empresa.logo }}</td>
            </tr>
            <tr>
                <th>Validation</th>
                <td>{{ empresa.validation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('empresas_index') }}">back to list</a>

    <a href="{{ path('empresas_edit', {'id': empresa.id}) }}">edit</a>

    {{ include('empresas/_delete_form.html.twig') }}
{% endblock %}
